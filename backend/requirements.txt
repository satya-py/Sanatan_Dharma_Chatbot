# --- Web framework ---------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
websockets>=12.0
jinja2>=3.1.3

# --- Settings / config -----------------------------------------------------
pydantic>=2.6.4
pydantic-settings>=2.2.1
python-dotenv>=1.0.1

# --- Database --------------------------------------------------------------
sqlalchemy>=2.0.28

# --- LangChain / agent stack ----------------------------------------------
langchain>=0.1.12
langchain-community>=0.0.28
langchain-core>=0.1.33
langchain-text-splitters>=0.0.1
langchain-groq>=0.1.3
langgraph>=0.0.26

# --- Retrieval -------------------------------------------------------------
# fastembed runs BAAI/bge-small-en-v1.5 and the ms-marco cross-encoder on
# onnxruntime instead of torch. Same vectors (cosine > 0.999999 vs the
# sentence-transformers build, so the pre-built FAISS indices stay valid) at
# roughly a quarter of the memory, and no multi-hundred-MB CUDA wheels at
# build time.
#
# Set EMBEDDING_BACKEND=torch and additionally install the packages listed in
# requirements-torch.txt to go back to the sentence-transformers path. That
# needs an instance with >= 2 GB RAM.
fastembed>=0.4.2
faiss-cpu>=1.8.0
numpy>=1.26.4,<2.0.0
rank_bm25>=0.2.2

# --- External services -----------------------------------------------------
requests>=2.31.0
tavily-python>=0.3.3
assemblyai>=0.23.1
gTTS>=2.5.1
langdetect>=1.0.9
